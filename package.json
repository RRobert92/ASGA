{
  "name": "ASGA",
  "version": "0.34.0",
  "description": "Automatic Spatial Graph Analysis",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "./Get_R_Win.sh",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --out=Dist",
    "package-win": "electron-packager . --overwrite --platform=win32 --arch=x64 --icon=docs/img/Icon.ico --out=Dist --version-string.CompanyName=CFCI --version-string.FileDescription=CE --version-string.ProductName=\"ASGA\"",
    "package-linux": "electron-packager . --overwrite --platform=linux --arch=x64 --icon=Cdocs/img/ASGA_logo.png --prune=true --out=Dist"
  },
  "repository": "https://github.com/RRobert92/ASGA",
  "keywords": [
    "Electron",
    "ASGA"
  ],
  "author": "Robert Kiewisz",
  "license": "GNU 3.0",
  "devDependencies": {
    "electron": "^12.0.0"
  },
  "dependencies": {
    "electron-packager": "^15.2.0"
  }
}
